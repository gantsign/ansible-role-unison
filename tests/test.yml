---
- hosts: localhost
  remote_user: root
  roles:
    - role: ansible-role-unison
      unison_include_directories:
        - included
      unison_include_files:
        - includeme
      unison_ignore_paths:
        - Name excluded
